<UserControl x:Class="Magma.Controls.GameCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Magma.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="192" d:DesignWidth="128" ClipToBounds="False">
    <Grid>
        <Border ClipToBounds="True" CornerRadius="{Binding BorderRadius, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Border.Background>
                <ImageBrush ImageSource="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}"></ImageBrush>
            </Border.Background>
        </Border>
        <Image Opacity="0.20" CacheMode="BitmapCache" Source="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Image.Effect>
                <BlurEffect Radius="80"/>
            </Image.Effect>
        </Image>
    </Grid>
</UserControl>
